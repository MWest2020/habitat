# Habitat architect — polish-supporter-mail

_run_id 20260904-211410-30373 · verdict ok · 2026-09-04T19:19:34Z_

---

{"verdict":"PASS","summary":"polish-supporter-mail is fully speced (design.md D1–D6, spec deltas, tasks.md all owner inputs resolved; T1 already done in a prior commit). This plan covers the remaining builder tasks T2–T6: the HTML-alternative credential mail, its tests, the NETNL_SUPPORTER_MIN_AMOUNT default bump to 2, docs, and D6 run-output evidence. No further owner decision is needed — the only open items are execution-craft judgment calls, called out under risks.","plan":[{"objective":"Add an HTML alternative to the credential mail (Mail.html, a static template, and multipart/alternative sending) without touching the plaintext body, the notify mail, or build_credential_mail's signature.","files":["src/netnl/mail.py"],"steps":["Add `html: str | None = None` to the `Mail` dataclass (mail.py:36-40) — every existing call site (RecordingSender tests, build_notify_mail) is then unaffected.","Import the stdlib `html` module under a different local name to avoid colliding with the new `html` field/variable, e.g. `import html as _html` at the top with the other imports (mail.py:13-20).","Add a new module-level constant `_CREDENTIAL_HTML_TEMPLATE` (static Python string, placed after `_CREDENTIAL_BODY_TEMPLATE`, mail.py:57-94) satisfying design.md D3/D4: single-column nested `<table>` layout ≤600px centred, `role=\"presentation\"` on layout tables, inline CSS plus exactly one `<style>` block in `<head>` for `@media (prefers-color-scheme: dark)`, `<meta name=\"color-scheme\" content=\"light dark\">` and `<meta name=\"supported-color-schemes\" content=\"light dark\">`, `lang=\"en\"`, one `<h1>`, system font stack for body text and a monospace stack for code, the credential in exactly one monospace block with `white-space: pre; word-break: keep-all` (no wrapping), the GitHub Actions and CLI snippets reproduced as `<pre>` blocks referencing \"the credential above\" (never re-interpolating the password), section order thanks → credential → what it is (best-effort/no-SLA/fair-use) → keep it safe → how to use it (Actions then terminal) → links, no emoji, no marketing copy, no `<img>`/`<link>`/`<script>`/`<form>`/`@import`/`url(`/`on*=` anywhere, and the only placeholders `{credential}` `{public_endpoint}` `{docs_url}` `{demo_url}` `{install_url}` (mirrors the shape sketched in design.md:101). If a frontend/design skill is available to this role, use it for typographic hierarchy, spacing and the code-block treatment only — not for imagery or motion (forbidden by D3).","In `build_credential_mail` (mail.py:97-116): keep the existing `body = _CREDENTIAL_BODY_TEMPLATE.format(...)` call byte-for-byte unchanged, then build `html_body = _CREDENTIAL_HTML_TEMPLATE.format(credential=_html.escape(credential, quote=True), public_endpoint=_html.escape(public_endpoint, quote=True), docs_url=_html.escape(_DOCS_URL, quote=True), demo_url=_html.escape(_DEMO_URL, quote=True), install_url=_html.escape(_INSTALL_URL, quote=True))`, and return `Mail(to=to, subject=_CREDENTIAL_SUBJECT, body=body, html=html_body)`. Do not add any parameter to the function signature.","Leave `build_notify_mail` (mail.py:119-126) untouched — it keeps returning a `Mail` with `html` defaulting to `None`.","In `smtp_sender`'s inner `_send` (mail.py:174-196), immediately after `message.set_content(mail.body)` add: `if mail.html is not None: message.add_alternative(mail.html, subtype=\"html\")`. Change nothing else in `_send` — the `except Exception` → `DeliveryError` root-fix, the `[mail.to]` envelope, and the starttls-before-login order must stay exactly as they are.","Extend the module docstring's \"only three values reach the mail\" paragraph (mail.py:1-11) to state the invariant now covers both the plaintext and the HTML MIME part."],"verification":"`uv run pytest tests/netnl/test_netnl_mail.py -q` passes once T3's tests exist. Separately, render one sample mail and eyeball it (this is also the D6 evidence, see the T6 task): `uv run python -c \"from netnl.mail import build_credential_mail as b; print(b(to='donor@example.org', username='supporter-demo0000', password='demo-password-1234', public_endpoint='https://facade.example.org').html)\"`.","out_of_scope":"No new interpolation surface or new build_credential_mail parameter (no donor name, note, or any other delivery field). No templating dependency (Jinja/MJML/build step) — plain str.format + html.escape only. No change to build_notify_mail."},{"objective":"Lock in the multipart shape, the escaping, the zero-network-request constraints, and the never-leaks-a-donor-field invariant for the HTML part, without weakening any existing plaintext test.","files":["tests/netnl/test_netnl_mail.py","tests/netnl/test_netnl_leak.py"],"steps":["Add a test that `mail.smtp_sender` given a `Mail(...)` built without `html` (i.e. `html=None`, the default) still produces a single-part message: parse the fake SMTP's captured `.sent` string with `email.message_from_string`, assert `is_multipart() is False` and `get_content_type() == \"text/plain\"`.","Add a test that sending a real `build_credential_mail(...)` result through `smtp_sender` produces a message where `is_multipart()` is True, `get_content_type() == \"multipart/alternative\"`, its two payload parts have content types `[\"text/plain\", \"text/html\"]` in that order, and the plaintext part's `get_content()` equals `mail.body`.","Add a test that the credential string (`f\"{username}:{password}\"`) appears exactly once in `mail.html`, and the raw password substring also appears exactly once (no second copy in the reproduced Actions/CLI snippets) — mirrors the existing `test_build_credential_mail_shows_a_single_credential_string_once` but against `.html`.","Add a test proving no field beyond the three interpolated values reaches `.html`: build with a distinctive sentinel-shaped value nowhere in the template's own text (reuse the spirit of `test_build_credential_mail_never_echoes_a_donor_supplied_field` — the TypeError-on-extra-kwarg test already proves this structurally for the plaintext body; add one direct assertion on `.html` too, e.g. that it contains none of a set of donor-like strings that were never passed in).","Add a test asserting `mail.html` contains none of, case-insensitively: `<script`, `<img`, `<link`, `<form`, `@import`, `url(`, or an inline event handler pattern (`on[a-z]+=`).","Add a URL-allowlist test: extract every `http://`/`https://` occurrence from `mail.html` (e.g. via regex) and assert each one starts with `_DOCS_URL`, `_DEMO_URL`, or the `public_endpoint` used to build the mail; separately confirm `_INSTALL_URL` (a `git+https://` string, not `http(s)://`) still appears verbatim in the CLI snippet.","Add an escaping test: build with `public_endpoint=\"https://x.example/?a=1&b=<2>\"` and assert `&amp;`, `&lt;`, `&gt;` appear in `mail.html` while the same string appears raw (unescaped) in `mail.body`.","Confirm (existing, unchanged) `build_notify_mail(...)` still has `.html is None` — add an explicit assertion for this if none exists yet.","In `tests/netnl/test_netnl_leak.py`, read `_assert_supporter_no_leak` and its callers first (test_netnl_leak.py:111-166) — it deliberately does NOT scan the sent `Mail` itself, only the HTTP response, log output and raw DB dump, because the mail legitimately carries the password and address (see the comment at test_netnl_leak.py:164-165). Extend `test_supporter_issuance_never_leaks_secrets_or_pii` to complete that same statement for the HTML part: after the existing `assert sent[0].to == _SUPPORTER_EMAIL` (line 166), add an assertion that `sent[0].html` is not `None` and legitimately contains `issued_password` — proving the earlier \"neither reaches the reply, the log, or the database\" claim (checked only against `resp`/`caplog`/the DB dump, never against `sent[0]`) now covers both MIME parts, not just the plaintext one."],"verification":"`uv run pytest tests/netnl/test_netnl_mail.py tests/netnl/test_netnl_leak.py -q` — all new and existing tests pass; no existing assertion is weakened or removed.","out_of_scope":"Do not touch `_assert_supporter_no_leak`'s haystack construction (resp/caplog/db) to include the sent mail — that would break the documented, intentional distinction between \"the mail legitimately carries this\" and \"nothing else may\"."},{"objective":"Move NETNL_SUPPORTER_MIN_AMOUNT's default from 0 to 2, and prove the new default actually reaches the qualification decision through real Settings, not just a hand-built config.","files":["src/netnl/settings.py","tests/netnl/test_netnl_supporter_settings.py"],"steps":["In `_load_supporter` (src/netnl/settings.py:376), change `_resolve_decimal(env, \"NETNL_SUPPORTER_MIN_AMOUNT\", Decimal(\"0\"))` to `Decimal(\"2\")`. `_resolve_decimal`, the `Decimal`-not-`float` parsing, and every existing validation (non-numeric, negative, non-finite) stay untouched.","In `tests/netnl/test_netnl_supporter_settings.py::test_defaults_match_design` (line 70), change `assert cfg.min_amount == Decimal(\"0\")` to `Decimal(\"2\")`.","Add a new test (near the other min_amount tests, after `test_min_amount_accepts_a_very_large_but_finite_value`) that: (a) loads real `Settings` via `load(_supporter_env(settings_env))` with `NETNL_SUPPORTER_MIN_AMOUNT` left unset, (b) builds a `bmc.QualifyConfig` from that loaded `s.supporter` the same way `tests/netnl/test_netnl_supporter.py:500` already does (`dataclasses.replace`/keyword construction from `cfg.accept_test_mode`, `cfg.min_amount`, `cfg.currency`), and (c) asserts `bmc.qualifies(bmc.parse_delivery(bmc_payload(data={\"amount\": \"1.99\"})), that_config) == bmc.Decision.IGNORE_AMOUNT`, and that amounts `\"2.00\"`, `\"2\"`, and `2` all reach `Decision.ISSUE` (or at minimum clear the amount check — use `bmc_payload()`'s defaults, which are already live/donation.created/a usable recipient, for the other fields)."],"verification":"`uv run pytest tests/netnl/test_netnl_supporter_settings.py tests/netnl/test_netnl_supporter.py tests/netnl/test_netnl_bmc.py -q` all green. Before touching `tests/netnl/test_netnl_supporter.py` or `tests/netnl/test_netnl_bmc.py`, re-grep both for `amount` to confirm the current state still holds: `bmc_payload`'s default `data[\"amount\"]` is `\"5.00\"` (tests/netnl/conftest.py:273, above the new floor), so every call site that omits an explicit amount override already clears 2 unmodified; the one explicit override in test_netnl_supporter.py (`data={\"amount\": \"1.00\"}`, line 160) is paired with a locally-set `min_amount=Decimal(\"10\")` and is unaffected by the settings default; `test_netnl_bmc.py`'s `_cfg()` helper (line 269) builds `QualifyConfig` by hand with its own hardcoded `min_amount=Decimal(\"0\")`, entirely independent of `netnl.settings`, so `test_qualifies_default_minimum_zero_accepts_any_amount` there is testing that helper's own default, not this change — it needs no edit. If that re-grep turns up something these notes missed, treat it as a normal fixture fix, not a design gap.","out_of_scope":"No change to `bmc.qualifies`/`QualifyConfig` (the \"at or above\" comparison already exists and is unchanged). No change to `NETNL_SUPPORTER_CURRENCY` handling. No tiers, memberships, or per-amount differences."},{"objective":"Update operator-facing docs and the changelog to describe the new default floor and the HTML-alternative mail, without rewriting settled history.","files":["deploy/.env.example","docs/how-to/supporter-webhook.md","docs/how-to/supporter-key.md","CHANGELOG.md"],"steps":["`deploy/.env.example`: locate the `NETNL_SUPPORTER_MIN_AMOUNT` line/comment and change its example value and default-describing comment to `2` (e.g. `# NETNL_SUPPORTER_MIN_AMOUNT=2` with a comment noting donations below the floor are acknowledged with 200 and ignored) — match the file's existing comment style for the other supporter variables.","`docs/how-to/supporter-webhook.md:116-119`: replace \"Every donation on the account qualifies by default (`NETNL_SUPPORTER_MIN_AMOUNT=0`) — raise it, or set `NETNL_SUPPORTER_CURRENCY`, if that default does not fit\" with wording stating the default floor is 2.00 or more (still overridable via `NETNL_SUPPORTER_MIN_AMOUNT`), `NETNL_SUPPORTER_CURRENCY` remaining optional as today. Add one or two sentences (near this section or wherever the credential mail is first mentioned in this doc) noting the credential mail is now sent as plaintext + an HTML alternative with zero remote resources.","`docs/how-to/supporter-key.md:23` (\"...gets back a `netnl` tenant credential...\" / \"a small donation\" framing): state the floor once — 2.00 or more, in whatever currency the BMC account reports — leave the rest of the page (fair-use table, revocation, manual fallback) unchanged.","`CHANGELOG.md`: under `## [Unreleased]`, add one new `### Changed` bullet (there is currently no `### Changed` heading under Unreleased — insert one before the first `### Fixed` at line 135, after the existing top `### Added` block, matching Keep a Changelog's Added→Changed→Fixed ordering) stating `NETNL_SUPPORTER_MIN_AMOUNT`'s default changed from 0 to 2. Add one new bullet to the top `### Added` block (near the top, line 9-10, matching that block's newest-first ordering) describing the HTML alternative and its no-remote-resources constraint in one sentence. Do NOT edit the existing `add-supporter-issuance` bullet's \"default 0, i.e. every donation\" text (CHANGELOG.md:34) — leave it as the historical record of what shipped that day; the new Changed bullet is where the corrected default lives, mirroring the same \"historical, not edited\" pattern already used for `add-supporter-issuance/design.md` per D5."],"verification":"Read each edited doc end-to-end: English throughout, no stray leftover \"default 0\"/\"every donation qualifies by default\" phrasing anywhere outside the historical CHANGELOG bullet. No new page created, so `docs/index.md` needs no edit.","out_of_scope":"No rewrite of `docs/how-to/supporter-webhook.md` or `supporter-key.md` beyond the floor value and the one-line HTML mention — same credential shape, same disclosure model, same links as today per the proposal's non-goals."},{"objective":"Produce the D6 run-output evidence: the full rendered HTML of one sample credential mail, and a full green scripts/verify.sh run, both in the run output (not committed as a repo file).","files":[],"steps":["After T2 and T3 land, render one sample credential mail with dummy values and print the full `.html` output to the run output, e.g.: `uv run python -c \"from netnl.mail import build_credential_mail as b; print(b(to='donor@example.org', username='supporter-demo0000', password='demo-password-1234', public_endpoint='https://facade.example.org').html)\"`.","Run `sh scripts/verify.sh` and include its full output in the run output."],"verification":"Both the rendered HTML and the `scripts/verify.sh` output appear verbatim in the run output; the HTML is complete enough to paste into a browser or an HTML-mail preview tool to check D3 (no external resources, dark-mode readability) and D4 (tone/structure/typographic hierarchy) by eye.","out_of_scope":"No file is committed for this evidence — it is run-output only, per design D6."}],"risks":["This architect session's own Bash access is blocked on paths under `deploy/` (\"commando raakt een secrets-/credential-pad\") — I could not read the current `deploy/.env.example` content directly and planned T5's first step from the proposal/design text alone (which already specifies the exact target line). This is a permission artifact of the architect role in this environment, not a project or spec issue; the builder role should have the access this task needs.","The exact HTML/CSS markup for `_CREDENTIAL_HTML_TEMPLATE` is a genuine craftsmanship judgment call within design.md's D3/D4 constraints (which are exhaustive but not a literal markup spec) — this is normal build-time work, not an owner decision, and design.md D4 explicitly invites using a frontend/design skill for it if the builder role has one available.","CHANGELOG.md currently has no `### Changed` heading under `[Unreleased]` and its `### Added` sections are ordered newest-first at the top, oldest at the bottom, with a second, unrelated `### Added` block further down (initial-commit entries) — the exact insertion points for the two new bullets are a formatting judgment call for the builder, laid out concretely in the plan above but not owner-mandated.","Task T4's verification assumes `tests/netnl/test_netnl_supporter.py` needs no fixture edits because `bmc_payload`'s default amount (`\"5.00\"`) already clears the new floor of 2 — this was confirmed by grepping the file's current state during planning, but the builder should re-confirm with a fresh grep before skipping that file, in case it has changed since this plan was written."]}
