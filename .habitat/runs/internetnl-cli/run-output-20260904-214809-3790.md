# Habitat architect — facade-followups

_run_id 20260904-214809-3790 · verdict ok · 2026-09-04T19:52:15Z_

---

{"verdict":"PASS","summary":"T1 (openspec artefacts) is already committed. Plan below covers the remaining builder tasks T2–T5 for facade-followups: the facade User-Agent wrapper in netnl/upstream.py, the known-tenant reservation two-tier cap in netnl/auth.py, CHANGELOG entries, and the verify.sh/evidence step. Executable as-is; no missing information found.","plan":[{"objective":"T2 — netnl/upstream.py: wrap every upstream opener so requests carry `User-Agent: netnl/<v> internetnl-cli/<v>` instead of the bare CLI token, without touching internetnl_cli.client.","files":["src/netnl/upstream.py","tests/netnl/test_netnl_requests.py"],"steps":["In src/netnl/upstream.py, import `from importlib.metadata import version` (or reuse `internetnl_cli.client._package_version` directly — it's already computed at CLI-module import time as `_USER_AGENT = f\"internetnl-cli/{_package_version()}\"`; importing that private helper is allowed per design D2 since it doesn't add a public re-export). Recommended simplest form: `from internetnl_cli.client import BatchClient, Opener, urllib_opener, _USER_AGENT, _package_version` and build `_FACADE_USER_AGENT = f\"netnl/{_package_version()} {_USER_AGENT}\"` — this reuses the CLI's own already-'unknown'-falling-back version string wholesale rather than recomputing it, and guarantees the same fallback behaviour by construction. If the private-import style is judged too tight a coupling, duplicate the four-line `_package_version` body in upstream.py instead, with a comment naming `internetnl_cli.client._package_version` as the original (design D2 explicitly allows either).","Add `_with_facade_user_agent(opener: Opener) -> Opener` immediately below the constant: a closure matching the exact `Opener` signature `(method, url, body, headers, timeout) -> HttpResponse`, that does `new_headers = dict(headers); new_headers[\"User-Agent\"] = _FACADE_USER_AGENT; return opener(method, url, body, new_headers, timeout)`. Must copy (`dict(headers)`), never mutate the `headers` dict `BatchClient._headers()` built (design D1) — mutating it in place would still work functionally here since `_headers()` builds a fresh dict per call, but copying is the pinned contract and is what makes the wrapper safe to reuse if that assumption ever changes.","Change `build_client` (currently `return BatchClient(build_config(settings), opener=opener or urllib_opener)`) to `return BatchClient(build_config(settings), opener=_with_facade_user_agent(opener or urllib_opener))`. This wraps both the real `urllib_opener` and any test-injected opener (e.g. `FakeOpener`), so `FakeOpener.calls` will record the facade UA without a network.","Do not touch `src/internetnl_cli/client.py`, `_USER_AGENT`, or `BatchClient._headers` — the CLI's own header stays `internetnl-cli/<v>` alone when the CLI is used directly, unwrapped.","Add a test to tests/netnl/test_netnl_requests.py (alongside the existing submit/status/results tests, which already drive requests through `fake_opener` and `tenant`): queue a reply with `queue_json`, make a request through `client` with `tenant['headers']`, then unpack `fake_opener.calls[0]` (same 5-tuple pattern used in tests/netnl/test_netnl_demo.py:83/138) and assert `headers['User-Agent'].startswith('netnl/')` and `'internetnl-cli/' in headers['User-Agent']`, and that `headers['Authorization']`, `headers['Content-Type'] == 'application/json'`, `headers['Accept'] == 'application/json'` are still present and unchanged — proves the wrapper copies rather than replaces."],"verification":"uv run pytest tests/netnl/test_netnl_requests.py tests/ -q -k \"opener or header or user_agent or client\" — the new test passes; run the full CLI test selection (any test file under tests/ exercising internetnl_cli.client directly, e.g. wherever BatchClient/_USER_AGENT is asserted) to confirm it is untouched and still green; `git diff --stat` shows no changes under src/internetnl_cli/.","out_of_scope":"internetnl_cli.client itself; the CLI's own User-Agent; any change to redirect refusal, request-id validation, or error handling in BatchClient."},{"objective":"T3 — netnl/auth.py: give known tenants (existing credential row, revoked or not) a second, separate failure-bucket cap so a burst against a real username can't hide behind 512 throwaway usernames exhausting the general cap.","files":["src/netnl/auth.py","tests/netnl/test_netnl_auth.py"],"steps":["Add `_MAX_TENANT_BUCKETS = 256` next to `_MAX_BUCKETS = 512` (auth.py:280-281), with a short comment (matching this module's existing 'Round-N fix' commentary style) explaining it reserves extra room only known tenants can occupy — reference design D3 rather than repeating its full rationale inline.","Change `_record_auth_failure`'s signature (auth.py:363-365) from `(conn, now, username, path)` to `(conn, now, username, path, *, known_tenant: bool = False)` — keyword-only, defaulting False so the existing positional call in tests/netnl/test_netnl_auth.py:289 (`auth._record_auth_failure(conn, clock(), f\"user-{i}\", \"/metadata/report\")`) keeps working unchanged.","Replace the cap check at auth.py:375 (`if key not in _auth_failure_buckets and len(_auth_failure_buckets) >= _MAX_BUCKETS:`) with the two-tier version from design D3: compute `limit = _MAX_BUCKETS + (_MAX_TENANT_BUCKETS if known_tenant else 0)` and compare against that instead of the bare `_MAX_BUCKETS`. Keep everything else in the function (the defensive re-sweep, the bucket increment) unchanged.","Update the three call sites inside `authenticate` (auth.py:388-433): the no/unparseable-header call (auth.py:417) and the unknown-username call (auth.py:424) stay as-is (default `known_tenant=False` — there is no known username, or none at all). The wrong-password-or-revoked call (auth.py:430, `if not ok or credential['revoked_at'] is not None:`) gets `known_tenant=True` added: `_record_auth_failure(conn, now, username, path, known_tenant=True)`. No new query — `credential` is already the row `store.find_credential` returned earlier at line 421, so 'known' is exactly 'a row was found', matching design D4 for both the wrong-password and revoked cases.","Update the block comment above `_MAX_BUCKETS` (auth.py:206-277, the long round-2/round-3 commentary on aggregator bounds) and any inline prose stating the bound as `_MAX_BUCKETS` plus route count: state the new bound as `_MAX_BUCKETS + _MAX_TENANT_BUCKETS + routes` per task 3.3. Keep the existing round-2/round-3 attribution labels intact; add the new point as its own paragraph rather than rewriting history.","In tests/netnl/test_netnl_auth.py, add tests near `test_bucket_count_is_capped_regardless_of_unique_usernames` (line 282): (a) fill `_MAX_BUCKETS` distinct unknown usernames on one route via `auth._record_auth_failure(conn, clock(), f\"user-{i}\", path)` as the existing test does, confirm one more unknown still folds to `<other>` and dict size is unchanged; then create a real tenant credential (the `tenant` fixture, or `add_test_credential` directly, from conftest.py) and drive a wrong-password failure through the HTTP layer (or call `auth._record_auth_failure(conn, clock(), tenant_username, path, known_tenant=True)` directly, matching how `authenticate` would call it) — assert a new bucket exists keyed on the sanitised tenant username, and after `_sweep_stale_auth_failure_buckets` an `auth-failure` audit row with `credential == tenant_username` exists alongside the `<other>` row. (b) Same for a revoked credential (`store.revoke_credential`) — treated as known the same way. (c) Fill `_MAX_BUCKETS + _MAX_TENANT_BUCKETS` entries using `known_tenant=True` calls (does not require `_MAX_TENANT_BUCKETS` distinct real DB rows — `_record_auth_failure` never itself queries `store`, `known_tenant` is just the boolean passed in, so synthetic usernames are fine here, unlike the HTTP-level test in (a)) then confirm the next known-tenant call folds into `<other>` — the reservation is itself capped. (d) Confirm `test_bucket_count_is_capped_regardless_of_unique_usernames` (unknown-only) still passes unmodified."],"verification":"uv run pytest tests/netnl/test_netnl_auth.py tests/netnl/test_netnl_leak.py -q — all pass, including the unmodified existing cap test and the new known-tenant tests.","out_of_scope":"Lifting the 512 cap; changing flush/sweep/batch-write mechanics or the 'aggregator write failure never fails the request' guarantee; any new configuration knob for either cap; changing what counts as 'known' beyond 'a credential row exists, revoked or not'."},{"objective":"T3b — tests/netnl/test_netnl_leak.py: confirm the wrong-password-is-now-known_tenant=True path still leaks nothing, without necessarily needing an edit.","files":["tests/netnl/test_netnl_leak.py"],"steps":["Check first whether an edit is even needed: tests/netnl/test_netnl_leak.py (test_upstream_401_never_leaks_credential etc.) covers *upstream* credential leakage on a 502, not the facade-tenant auth-failure audit path — that path is already covered by test_password_never_appears_in_audit_row in tests/netnl/test_netnl_auth.py (line 230), which authenticates with a wrong password for an existing tenant and is unaffected by which cap tier the failure lands in, since the password itself is never part of the key or the audit row on either tier.","If no gap is found (expected outcome), leave this file untouched — task 3.5's 'extend the parametrisation if it enumerates paths' does not apply here since this file does not enumerate auth-failure code paths.","If a gap is found during implementation (e.g. a new path that logs something auth-adjacent), extend the existing `_assert_no_leak` pattern rather than adding a new helper."],"verification":"uv run pytest tests/netnl/test_netnl_leak.py tests/netnl/test_netnl_auth.py -q — both green; `git diff` shows this file is untouched unless a genuine gap was found and documented in the PR description.","out_of_scope":"Any change to the upstream-credential-leak assertions this file already makes."},{"objective":"T4 — CHANGELOG.md: record the facade User-Agent (Added) and the known-tenant reservation (Changed).","files":["CHANGELOG.md"],"steps":["Under `## [Unreleased]`, add one bullet to the existing `### Added` section (CHANGELOG.md:9) describing the facade User-Agent: every upstream call now carries `User-Agent: netnl/<version> internetnl-cli/<version>` so facade traffic is distinguishable from a directly-run CLI in the upstream instance's logs; note it's a pure header addition, same body/Authorization/redirect-refusal/error discipline.","Insert a new `### Changed` subsection between the existing `### Added` (ends CHANGELOG.md:133) and `### Fixed` (CHANGELOG.md:135), per Keep a Changelog's conventional section ordering (Added, Changed, ..., Fixed). Add one bullet: known tenants (an existing credential row, revoked or not) now keep their own failed-authentication bucket past the general 512-pair cap, up to a further 256-entry reservation — closes `add-measurement-api`'s residual risk N2. State that an unknown username still folds into the `<other>` overflow record exactly as before, and that a credential value on an `auth-failure` row is still attacker-supplied, never verified attribution.","Match this file's existing voice: full sentences, present tense for behaviour, references to the change name in backticks where prior entries do the same (e.g. `(openspec/changes/facade-followups)`), no marketing language."],"verification":"English throughout (task 4.1's stated check); manual read confirms both bullets accurately describe the shipped behaviour (not the design doc's aspirational wording) once T2/T3 are implemented.","out_of_scope":"Rewriting or renumbering any existing CHANGELOG entry; touching add-measurement-api's own design.md N2 paragraph (D5: that stays historical, unedited)."},{"objective":"T5 — Evidence: prove the build is green and show the header actually being set.","files":[],"steps":["Run `sh scripts/verify.sh` and capture its output in the run output (task 5.1).","From the T2 test added above, capture one recorded `FakeOpener` header dict in the run output, with the credential value redacted (the `Authorization` value is base64 of `username:password` — replace it with a placeholder like `Basic <redacted>` before pasting, do not paste the real base64 token even though it's test-only data) (task 5.2)."],"verification":"sh scripts/verify.sh exits 0; the pasted header dict shows `User-Agent` starting with `netnl/` and containing `internetnl-cli/`.","out_of_scope":"Any script or CI config change — scripts/verify.sh itself is not part of this change."}],"risks":["The plan-format skill at .claude/skills/plan-format could not be read — every access attempt (Read, Bash ls/cat/grep) was blocked by a hook reporting 'secrets-/credential-pad geblokkeerd' (a secrets/credential-path guard), even for directory listing. This looks like an overly broad hook match rather than an intentional restriction on this specific skill; worth checking the hook's path pattern. In its absence this plan uses the objective/files/steps/verification/out_of_scope structure the architect role's own instructions already require, which should be equivalent in substance.","Design D2's preferred header-construction approach (reuse `internetnl_cli.client._package_version`, a private helper) versus duplicating its four lines is left as a builder choice per the design doc itself — flagging per the architect's escalation rule rather than picking, though I noted a recommended concrete form (reusing `_USER_AGENT` wholesale) since it best satisfies 'same fallback behaviour by construction' with the least new code.","T3's test step (a) needs a real DB-backed credential (via the `tenant`/`add_test_credential` fixtures) to prove `known_tenant` is correctly derived from `store.find_credential` at the `authenticate()` call-site level, while test step (c) (capping the reservation itself) can use synthetic `known_tenant=True` calls since `_record_auth_failure` takes the boolean directly — flagged in the steps so the builder doesn't try to create 256 real DB rows unnecessarily."]}
