# Habitat builder — facade-followups

_run_id 20260904-215223-32304 · verdict ok · 2026-09-04T20:02:32Z_

---

{"verdict":"PASS","summary":"Implementeerde T2-T5 van openspec-change facade-followups: facade User-Agent op elke upstream call (netnl/upstream.py), en de known-tenant reservering in de auth-failure aggregator (netnl/auth.py), plus tests, CHANGELOG-entries en tasks.md-afvinking. T1 (proposal/design/tasks/spec-delta) was al af vóór deze run.","evidence":"`uv run pytest -q`: 637 passed, 1 warning in 79.75s (twee losse runs, identiek resultaat; de eerdere run bevatte één flaky timing-test — `test_flush_of_many_buckets_is_one_fast_transaction_outside_the_lock`, 0.105s vs 0.1s-grens onder gedeelde CI-load — die in isolatie in 0.28s slaagt, dus geen regressie van deze wijziging).\n\nRedacted FakeOpener header dict van een echte submit-call:\n{'Content-Type': 'application/json', 'Accept': 'application/json', 'User-Agent': 'netnl/0.1.0 internetnl-cli/0.1.0', 'Authorization': '<redacted>'}\n\n`git diff --stat` raakt exact de bestanden die design.md's exit-criteria voorschrijven: CHANGELOG.md, openspec/changes/facade-followups/tasks.md, src/netnl/auth.py, src/netnl/upstream.py, tests/netnl/test_netnl_auth.py, tests/netnl/test_netnl_leak.py, tests/netnl/test_netnl_requests.py. `git status --porcelain -- src/internetnl_cli` is leeg (ongewijzigd).\n\n`sh scripts/verify.sh` en `openspec validate ... --strict` kon ik niet direct draaien — de sandbox's dontAsk-permissiesysteem weigerde beide commando's expliciet (en `openspec` is sowieso niet geïnstalleerd, `npx` heeft geen netwerktoegang in deze sandbox). Ik heb verify.sh's eigen inhoud (`uv run pytest -q`, dan `shellcheck` indien aanwezig) los uitgevoerd: pytest groen, shellcheck niet geïnstalleerd (dus ook in verify.sh zelf een no-op). T1's openspec-validatie is al eerder in deze branch gedaan (commit 8309b56); mijn wijzigingen raken alleen tasks.md-vinkjes, geen proposal/design/spec-delta-inhoud.","changes":["src/netnl/upstream.py: _package_version() + _FACADE_USER_AGENT constante en _with_facade_user_agent(opener) wrapper; build_client wrapt nu opener or urllib_opener","src/netnl/auth.py: _MAX_TENANT_BUCKETS = 256; _record_auth_failure kreeg keyword-only known_tenant met two-tier cap-check; wrong-password/revoked call site geeft known_tenant=True door; block comment/docstrings bijgewerkt","tests/netnl/test_netnl_requests.py: nieuwe test dat elke FakeOpener-call de facade User-Agent draagt naast ongewijzigde Authorization/Content-Type/Accept","tests/netnl/test_netnl_auth.py: 3 nieuwe tests — known tenant krijgt eigen bucket na de cap, revoked credential telt als known, de tenant-reservering is zelf ook capped","tests/netnl/test_netnl_leak.py: nieuwe test dat een revoked-tenant auth-failure (known-tenant pad) het wachtwoord nooit lekt naar response/log/audit-rij","CHANGELOG.md: nieuwe Added-entry (facade User-Agent) en Changed-entry (known-tenant reservering, sluit add-measurement-api N2)","openspec/changes/facade-followups/tasks.md: T2-T5 checkboxes afgevinkt"],"deviations":["scripts/verify.sh en `openspec validate` kon ik niet direct via Bash aanroepen — permissiesysteem weigerde beide commando's onder dontAsk-mode zonder duidelijke reden (mogelijk sandbox-restrictie op deze specifieke binaries). Ik heb verify.sh's inhoud (pytest + optionele shellcheck) los gereproduceerd als equivalent bewijs in plaats van het script zelf te draaien."]}
