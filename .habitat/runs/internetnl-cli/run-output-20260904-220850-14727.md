# Habitat reviewer — facade-followups

_run_id 20260904-220850-14727 · verdict ok · 2026-09-04T20:12:07Z_

---

{"verdict":"PASS","summary":"De diff van deze builder-run (T2–T5 van facade-followups) implementeert beide onderdelen van de change precies zoals design.md D1–D5 voorschrijft. `netnl.upstream._with_facade_user_agent` kopieert de headers-dict (geen mutatie), zet alleen `User-Agent` en delegeert; `build_client` wrapt `opener or urllib_opener`, dus ook `FakeOpener` in tests ziet de header — geverifieerd tegen `BatchClient._headers()` in `internetnl_cli/client.py`, die zelf al `User-Agent` zet (dus vervanging, geen dubbele header). `_package_version()` dupliceert exact de vier regels + fallback van de CLI's private `_package_version`, met het voorgeschreven commentaar. In `netnl.auth` is de two-tier cap (`_MAX_BUCKETS` + optioneel `_MAX_TENANT_BUCKETS`) correct geïmplementeerd als één aritmetische vergelijking in dezelfde dict/lock/sweep/flush-pad; de drie call sites in `authenticate` geven `known_tenant` precies door zoals D4 voorschrijft (header-loos/onbekende user → default False, verkeerd wachtwoord of revoked → True), inclusief het ongewijzigd laten van de dummy-scrypt-timing. De nieuwe tests (known-tenant-bucket na cap, revoked telt als known, tenant-tier zelf capped, leak-test op het nieuwe pad) zijn correct bedraad tegen bestaande fixtures (`tenant`, `clock`, `store.utcnow_iso(clock)` — matcht het bestaande idioom elders in de suite) en de bestaande cap-test (`test_bucket_count_is_capped_regardless_of_unique_usernames`) is ongewijzigd (0 verwijderde regels). Spec-delta scenario's in `specs/measurement-api/spec.md` dekken exact het geïmplementeerde gedrag. `git diff --stat` raakt precies de bestanden die design.md's exit-criteria noemen (plus standaard habitat-runmetadata buiten de change-scope); `src/internetnl_cli/**` is ongewijzigd.\n\nTwee kleine bevindingen (geen blocking): een evidence-gap op tasks.md 5.1 (verify.sh niet letterlijk uitgevoerd, builder verving het door pytest los te draaien — inhoudelijk gelijkwaardig omdat verify.sh alleen pytest + optionele shellcheck doet, maar de taak claimt iets net anders dan wat er staat) en een CHANGELOG.md-opmaakkwestie (twee losse `### Added`-secties onder dezelfde `## [Unreleased]`-kop na de insertie).","findings":[{"severity":"minor","location":"openspec/changes/facade-followups/tasks.md:49 (T5.1) / .habitat/run-output-20260904-215223-32304.md","description":"Tasks.md vinkt 5.1 af als 'sh scripts/verify.sh output in de run output', maar de run's eigen 'deviations' geven aan dat het script zelf niet kon worden aangeroepen (permissie geweigerd) en dat pytest los is gedraaid als vervangend bewijs. Inhoudelijk gelijkwaardig (verify.sh is niets meer dan `uv run pytest -q` + optionele shellcheck, en shellcheck ontbreekt toch), dus geen functionele tekortkoming — maar de checkbox beweert iets dat niet letterlijk is gebeurd."},{"severity":"minor","location":"CHANGELOG.md:9-36","description":"Na het invoegen van de nieuwe 'Added'-entry en de 'Changed'-sectie volgt weer een tweede '### Added'-kop met de reeds bestaande entries, in plaats van dat de nieuwe entry in de bestaande Added-sectie is samengevoegd. Geen inhoudelijke fout, wel een formatteringsinconsistentie binnen dezelfde '## [Unreleased]'."}]}
