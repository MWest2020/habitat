# Habitat security — facade-followups

_run_id 20260905-135905-28227 · verdict ok · 2026-09-05T12:01:30Z_

---

{"verdict":"PASS","summary":"Security review of the facade-followups builder run (commit 5c00cb8 on top of main) covering src/netnl/upstream.py and src/netnl/auth.py. Both changes are small, well-scoped, and match design.md D1–D4 exactly. (A) The User-Agent wrapper only inserts a hardcoded, non-attacker-controlled string (package version via importlib.metadata, with the same safe except-Exception fallback as internetnl_cli.client._package_version) and copies the headers dict rather than mutating it — no injection surface, no behavior change to Authorization/body/redirect handling. (B) The known-tenant reservation only sets known_tenant=True at the one call site gated behind store.find_credential(...) succeeding — i.e. only for usernames that already match an existing credentials row (revoked or not) — so an attacker cannot force arbitrary/unknown usernames into the reserved tier. The reserved tier is itself hard-capped at _MAX_TENANT_BUCKETS=256, so total dict growth stays bounded (_MAX_BUCKETS + _MAX_TENANT_BUCKETS + routes), preserving the existing DoS/memory-growth mitigation this aggregator exists for. No new SQL queries, no new logging of secrets, no timing-oracle changes (the dummy-scrypt path for unknown usernames is untouched). Tests added cover exactly the design's \"Testing constraints\" (own bucket past cap, revoked-counts-as-known, tenant tier itself capped, no password leak on the new path). No secrets or credentials found in the newly committed .habitat/run-report/audit artifacts (Authorization is redacted in the recorded evidence). No blocking or major findings.","findings":[{"severity":"minor","location":"src/netnl/auth.py:22-49 (design.md D3) — _MAX_TENANT_BUCKETS reservation","description":"The 256-slot known-tenant reservation assumes the operator's real tenant count stays comfortably under 256 (documented explicitly as an assumption in proposal.md: 'operator-sized tenant set'). If an operator ever exceeds roughly 256 active credential rows, an attacker who already knows more than 256 real usernames could fill the reserved tier and push additional real tenants' failures back into the unattributed `<other>` overflow bucket, quietly reducing the audit-attribution guarantee this change is meant to provide. This is a documented design assumption rather than a coding defect, and exploiting it still requires the attacker to already know many real usernames — the same prerequisite the mitigation itself is built around — so it is not blocking. Flagging only so it stays visible if the tenant set ever grows past the assumed small, operator-sized scale."}]}
