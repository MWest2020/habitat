# Habitat architect — polish-supporter-mail

_run_id 20260905-154410-2197 · verdict ok · 2026-09-05T13:47:16Z_

---

{"verdict":"PASS","summary":"polish-supporter-mail is fully specced (proposal/design/tasks/spec-delta all present, T1 already ticked) and directly executable. Below is the builder plan for the remaining work: T2+T3 (HTML alternative in netnl.mail, with tests), T4 (NETNL_SUPPORTER_MIN_AMOUNT default 0→2, with tests), T5 (docs/.env.example/CHANGELOG), T6 (evidence). No owner decision is outstanding and no design ambiguity was found; one note below is a verified simplification, not a risk.","plan":[{"objective":"Give the credential mail an HTML alternative alongside today's plaintext, with the interpolation invariant (only credential/public_endpoint/three static URLs, HTML-escaped) enforced structurally and tested.","files":["src/netnl/mail.py","tests/netnl/test_netnl_mail.py"],"steps":["mail.py: add `import html as _html` near the top (stdlib `html` module name collides with the new `Mail.html` field/local variable — do not name a local variable `html`; use `html_body` for the rendered string).","mail.py: add `html: str | None = None` to the `Mail` dataclass (line ~36-40). Every existing call site (`build_notify_mail`, `RecordingSender`, existing tests constructing `Mail(...)` positionally-by-keyword) is unaffected because it is optional and defaults to `None`.","mail.py: add a module-level `_CREDENTIAL_HTML_TEMPLATE` (a plain triple-quoted string, `str.format` placeholders `{credential} {public_endpoint} {docs_url} {demo_url} {install_url}` — same five names the design.md snippet uses). It must satisfy every constraint in design.md D3/D4, each of which has a corresponding test in the next step: `<meta name=\"color-scheme\" content=\"light dark\">` and `<meta name=\"supported-color-schemes\" content=\"light dark\">` in `<head>`; one `<style>` block for `@media (prefers-color-scheme: dark)` overrides; everything else inline `style=\"...\"`; nested `<table role=\"presentation\">` layout, single column, `max-width: 600px`, centred; system font stack for body text, monospace stack for the credential and the two code blocks; the credential in exactly one monospace block with `white-space: pre` and `word-break: keep-all` so `supporter-<hex>:<password>` never wraps mid-string; the GitHub Actions and CLI/terminal blocks reproduced as `<pre>` with the same content as the plaintext template (YAML indentation preserved), both referencing \"the credential above\" rather than re-interpolating `{credential}`; one `<h1>`; `lang=\"en\"` on `<html>`; no `<img>`, `<link>`, `<script>`, `<form>`, `@import`, `url(...)`, or `on*=` attribute anywhere; the only URLs in the document are `{public_endpoint}`, `{docs_url}`, `{demo_url}`, `{install_url}` (the last is a `git+https://` string); section order mirrors the plaintext (thanks → credential → what it is → keep it safe → how to use it → links); no emoji, no marketing copy.","mail.py: in `build_credential_mail`, after computing `body` exactly as today, render `html_body = _CREDENTIAL_HTML_TEMPLATE.format(credential=_html.escape(credential, quote=True), public_endpoint=_html.escape(public_endpoint, quote=True), docs_url=_html.escape(_DOCS_URL, quote=True), demo_url=_html.escape(_DEMO_URL, quote=True), install_url=_html.escape(_INSTALL_URL, quote=True))` and return `Mail(to=to, subject=_CREDENTIAL_SUBJECT, body=body, html=html_body)`. Do not add any parameter to `build_credential_mail`'s signature — that is the structural guarantee the existing `test_build_credential_mail_never_echoes_a_donor_supplied_field` test (test_netnl_mail.py:132) already relies on via `TypeError` on an extra kwarg.","mail.py: leave `build_notify_mail` untouched — `Mail`'s new field defaults to `None`, so its return already satisfies 'no HTML alternative'.","mail.py: in `smtp_sender`'s inner `_send` (line ~174-196), immediately after `message.set_content(mail.body)` add `if mail.html is not None: message.add_alternative(mail.html, subtype=\"html\")`. Nothing else in `_send` changes: the `except Exception -> DeliveryError` wrapping, the `[mail.to]` envelope, and starttls-before-login order are untouched (do not reorder or restructure the surrounding try/with block).","mail.py: extend the module docstring's 'only three values reach the mail' paragraph (lines 1-11) to state the invariant now covers both MIME parts, not just the plaintext body.","test_netnl_mail.py: add/extend tests for — (a) `Mail(...)` without `html` still yields a single-part `text/plain` message via `smtp_sender` (assert `not message.is_multipart()` and content type on the captured `_FakeSMTP.sent`/reconstructed `EmailMessage`); (b) a credential mail sent via `smtp_sender` is `multipart/alternative` with parts `[text/plain, text/html]` in that order, and the plaintext part equals `mail.body` byte-for-byte; (c) the credential string and the bare password substring each appear exactly once in the HTML (mirroring the existing plaintext test at test_netnl_mail.py:95-109); (d) reuse the sentinel/no-donor-field approach from `test_build_credential_mail_never_echoes_a_donor_supplied_field` against `m.html`; (e) HTML contains none of `<script`, `<img`, `<link`, `<form`, `@import`, `url(`, or a case-insensitive ` on[a-z]+=` pattern; (f) every `http://`/`https://` substring in the HTML is one of `_DOCS_URL`, `_DEMO_URL`, `_INSTALL_URL`, or the passed `public_endpoint`; (g) a `public_endpoint` of `https://x.example/?a=1&b=<2>` appears HTML-escaped (`&amp;`, `&lt;`, `&gt;`) in `m.html` and verbatim in `m.body`; (h) `build_notify_mail(...).html is None`.","Getting the fake SMTP connection's sent message parts: parse `conn.sent` (the string passed to `sendmail`) with `email.message_from_string` (or `email.parser.Parser`) and use `walk()`/`get_payload()` to inspect `Content-Type` and part bodies — do not hand-roll MIME boundary parsing."],"verification":"`uv run pytest tests/netnl/test_netnl_mail.py -q` passes, including every existing test unchanged (no existing assertion in that file is weakened or removed) plus the new ones above.","out_of_scope":"No new interpolation surface (no donor name/note/personalisation — design non-goal). No templating dependency (Jinja/MJML/build step) — stdlib `str.format` + `html.escape` only, exactly like the plaintext template. Do not touch `build_notify_mail`'s content or the operator-notify path at all."},{"objective":"Move the minimum qualifying donation amount default from 0 to 2 (Decimal, 'at or above'), per owner decision O1/D5, and prove the new default actually reaches the qualification decision end to end.","files":["src/netnl/settings.py","tests/netnl/test_netnl_supporter_settings.py","tests/netnl/test_netnl_supporter.py"],"steps":["settings.py: in `_load_supporter` (line ~376), change `min_amount = _resolve_decimal(env, \"NETNL_SUPPORTER_MIN_AMOUNT\", Decimal(\"0\"))` to `Decimal(\"2\")`. `_resolve_decimal` itself (lines 205-228, decimal parsing, non-finite/negative rejection) is unchanged.","test_netnl_supporter_settings.py: change the assertion at line 70 (`assert cfg.min_amount == Decimal(\"0\")` inside `test_defaults_match_design`) to `Decimal(\"2\")`.","test_netnl_supporter_settings.py: add a new test that, with `NETNL_SUPPORTER_MIN_AMOUNT` unset, builds a `QualifyConfig` from a real `load(...)`-produced `Settings` (not a hand-made config) and asserts `bmc.qualifies` returns `IGNORE_AMOUNT` for a live `donation.created` of `1.99` and `ISSUE` for `2.00`, `\"2\"`, and `2` — this is the test that proves the default actually reaches the decision, not just the dataclass field.","test_netnl_supporter.py: run the full file against the new default first — `bmc_payload()` in conftest.py already defaults `amount` to `\"5.00\"` (conftest.py:273), and the one test that exercises a below-minimum amount (`test_amount_below_minimum_is_ignored`, line 150) already overrides `min_amount` to `Decimal(\"10\")` explicitly via `dataclasses.replace`, so on inspection no fixture in this file appears to rely on the old default of `0` to get a delivery issued. Confirm this by running the suite; only if a specific test fails because it implicitly relied on `0`, fix it by raising that payload's amount to `>= 2` (do not set `NETNL_SUPPORTER_MIN_AMOUNT=0` in `supporter_env` — the point of this change is to exercise the real default)."],"verification":"`uv run pytest tests/netnl/test_netnl_supporter_settings.py tests/netnl/test_netnl_supporter.py tests/netnl/test_netnl_bmc.py -q` passes.","out_of_scope":"No change to `NETNL_SUPPORTER_CURRENCY` (stays optional, untouched). No tiers/memberships. No retroactive revocation of keys already issued under the old default — that is an operator `netnl-admin user revoke` decision, not part of this change."},{"objective":"Bring docs, the deploy example env, and the changelog in line with the new default and the HTML mail — no behaviour change, text only.","files":["deploy/.env.example","docs/how-to/supporter-webhook.md","docs/how-to/supporter-key.md","CHANGELOG.md"],"steps":["deploy/.env.example: set `NETNL_SUPPORTER_MIN_AMOUNT` to a commented `# NETNL_SUPPORTER_MIN_AMOUNT=2` with a comment that donations below this are acknowledged (200) and ignored, no state written.","docs/how-to/supporter-webhook.md: replace the 'Live' section's sentence (currently 'Every donation on the account qualifies by default (`NETNL_SUPPORTER_MIN_AMOUNT=0`) — raise it...', around line 116-119) with the new floor (default 2.00 or more, account currency, 'at or above'); also mention in this doc that the credential mail now sends as plaintext + HTML with zero remote resources (tie to the new spec requirement).","docs/how-to/supporter-key.md: in the opening paragraph (around line 22-23, 'Someone makes a small, one-off donation...') state the floor once ('2.00 or more, in the account's currency') without changing anything else on the page — the model description, the fair-use table, and the manual-issuance fallback stay as-is.","CHANGELOG.md: under `## [Unreleased]`, add one `### Changed` entry (the default floor, 0 → 2) and one `### Added` entry (HTML alternative for the credential mail; state its constraints — zero remote resources, plaintext remains source of truth — in one sentence each)."],"verification":"Manual read-through: English throughout, no new page created (so no `docs/index.md` edit needed), and every place that stated the old '0'/'every donation qualifies' default in these four files now states '2.00 or more'.","out_of_scope":"Do not touch docs/how-to/deploy-facade.md, beta.md, or ci.md — none of them state the qualification default or the mail's MIME shape."},{"objective":"Produce the evidence the design requires (rendered sample HTML mail, since neither the reviewer nor the owner can open a mail client on a K8s Job) and run the repo's full gate.","files":[],"steps":["After T2/T3/T4/T5 are done, in the same session build one sample credential mail with dummy values, e.g. `python -c \"from netnl.mail import build_credential_mail; m = build_credential_mail(to='donor@example.org', username='supporter-aaaa1111', password='s3cr3t-pw', public_endpoint='https://facade.example.org'); print(m.html)\"` (or an equivalent `uv run` invocation) and include the full printed HTML verbatim in the run output — it is evidence, not a repo artefact, so it must not be committed as a file.","Run `sh scripts/verify.sh` (the full `uv run pytest -q` suite) and include its output in the run output.","Run `openspec validate polish-supporter-mail --strict` and `openspec validate --all --strict`."],"verification":"Run output contains: the rendered sample HTML in full, a green `scripts/verify.sh`, and both `openspec validate` commands passing.","out_of_scope":"No committed HTML fixture file or snapshot artefact — the design is explicit that this is run-output evidence only."}],"risks":["Design and tasks.md are already this detailed (down to exact template placeholder names and test names) — the architect's plan adds file/line grounding but makes no independent design decisions; if a builder finds the HTML template constraints under-specified in practice (e.g. exact colour values for the 4.5:1 contrast requirement), that is a legitimate implementation judgment call within D3/D4, not an owner escalation.","T4's plan step assumes, based on reading conftest.py (bmc_payload default amount '5.00') and test_netnl_supporter.py (the one below-minimum test already overrides min_amount explicitly), that no fixture needs an amount bump when the default changes 0→2. This is a reasonable read of the current test file but should be confirmed by actually running the suite rather than assumed — flagged as a plan step, not skipped."]}
