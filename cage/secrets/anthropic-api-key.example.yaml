# SPDX-License-Identifier: EUPL-1.2
# TEMPLATE — placeholders, geen echte waarde. Kopieer naar anthropic-api-key.enc.yaml,
# vul de echte key en versleutel met `sops -e -i` (zie README.md). Commit nooit plaintext.
apiVersion: v1
kind: Secret
metadata:
  name: anthropic-api-key
  namespace: agents
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "sk-ant-PLACEHOLDER"
